# define your env variables for the test env here
KERNEL_CLASS='App\SharedContext\Infrastructure\Framework\Symfony\Kernel'
APP_SECRET='$ecretf0rt3st'
SYMFONY_DEPRECATIONS_HELPER=999999

# DATABASE_URL is injected via compose.ci.yaml in CI
# For local test, override in .env.test.local (gitignored)